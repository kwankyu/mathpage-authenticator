[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mathpage_authenticator"
description = "Authentication by MathPage"
version = "1.2"
authors = [
  { name="Kwankyu Lee", email="ekwankyu@gmail.com" },
]
dependencies = [
    "jupyterhub",
]
